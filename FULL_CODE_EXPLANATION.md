# Полная Техническая Документация Проекта "Smart Home"

## 1. Обзор Системы

Система "Умный Дом" представляет собой комплексное IoT решение, объединяющее микроконтроллеры (ESP32), центральный сервер (Go), веб-интерфейс (Next.js) и мобильное приложение (Flutter).

### Архитектура
Ниже представлена визуальная схема взаимодействия всех компонентов системы:

```text
       ПОЛЬЗОВАТЕЛИ (КЛИЕНТЫ)                   СЕРВЕР (МОЗГ)                     ЖЕЛЕЗО (ИСПОЛНИТЕЛИ)
========================================================================================================

+---------------------+                  +-------------------------+
|  📱 Mobile App      |                  |                         |                  +----------------+
|     (Flutter)       | --- HTTP ---->   |      GO BACKEND         |  <--- HTTP ---   |  📟 ESP32 #1   |
|                     |                  |      SERVER             |     (Update)     |  (Sensors/Sec) |
+---------------------+                  |      :8080              |                  +----------------+
                                         |                         |                  | - Temp/Hum     |
+---------------------+                  |   [ In-Memory State ]   |                  | - Relay (AC)   |
|  💻 Web Dashboard   | --- HTTP ---->   |           |             |                  | - RFID Reader  |
|     (Next.js)       |                  |   [ JSON Database ]     |                  +----------------+
+---------------------+                  |                         |
                                         |                         |                  +----------------+
+---------------------+                  |                         |   --- HTTP --->  |  💡 ESP32 #2   |
|  🗣️ Yandex Alice    | -- Webhook -->   |                         |   (Poll Cmd)     |   (Lighting)   |
|     (via Ngrok)     |                  +-------------------------+                  +----------------+
+---------------------+                                                               | - 6x LEDs      |
                                                                                      | - LCD Display  |
                                                                                      +----------------+
```

### Описание потоков данных:

---

## 2. Backend (Go)
**Путь:** `GolandProjects/awesomeProject/main.go`

Сервер написан на **Go** с использованием фреймворка **Gin**. Он является центральным хабом, который хранит состояние системы и синхронизирует все устройства.

### Основные Функции:
1.  **Хранение Состояния (State Management):**
    *   Хранит в памяти (RAM) текущие данные: `latestData` (температура, влажность), состояние реле `targetRelay` и ламп `lampCommands`.
    *   **Файловая БД:**
        *   `cards.json`: Список разрешенных RFID карт.
        *   `data.json`: История показаний датчиков.
2.  **Эмуляция Погоды и Солнечных Панелей:**
    *   Фоновая горутина `updateWeather()` периодически обновляет фейковые данные для солнечных панелей (зависит от времени суток) и генерирует прогноз погоды.
3.  **API Endpoints:**
    *   `POST /update`: Принимает данные от ESP32 Main, сохраняет их и отдает команды (реле).
    *   `GET /api/esp2/state`: Отдает команды управления лампами для ESP2 Lighting.
    *   `GET /api/status`: Основной эндпоинт для клиентов (Web/Mobile), возвращает полный слепок состояния системы.
    *   `POST /api/auth/card`: Проверяет UID карты. Если карта есть в `cards.json`, включает свет и пускает пользователя.
    *   `POST /api/lamp/:id/toggle` и `set`: Управление конкретной лампой.
    *   `POST /api/lamps/all`: Включить/Выключить всё.

---

## 3. Hardware (Аппаратная Часть)

### 3.1. ESP32 Main (Сенсоры и Безопасность)
**Путь:** `esp32/src/main.cpp`
**Роль:** Сбор данных и контроль доступа.

*   **Датчики:**
    *   `DHT11` (Pin 4): Температура и Влажность воздуха.
    *   `Soil Moisture` (Pin 34): Влажность почвы (аналоговый).
*   **Исполнительные устройства:**
    *   `Relay` (Pin 26): Управление мощной нагрузкой (кондиционер/обогреватель). Работает в инвертированном режиме (`LOW` = ВКЛ).
*   **RFID (RC522):**
    *   Подключен по SPI (Pins 5, 18, 19, 23).
    *   При обнаружении карты отправляет её UID на `POST /api/auth/card`.
*   **Логика:**
    *   В цикле `loop()` каждые 2 секунды считывает датчики и отправляет JSON на сервер (`POST /update`).
    *   В ответе от сервера получает состояние реле (`command_relay`) и применяет его.

### 3.2. ESP32 Lighting (Свет и Экран)
**Путь:** `esp2/src/main.cpp`
**Роль:** Управление освещением зон и вывод информации.

*   **Освещение:**
    *   Управляет 6-ю каналами света (Pins 12, 13, 14, 27, 26, 25).
*   **Дисплей:**
    *   LCD 1602 (I2C) выводит температуру и влажность, полученные от сервера.
*   **Логика:**
    *   Работает в режиме "Poling Client". Постоянно делает `GET /api/esp2/state`.
    *   Получает массив `lamp_commands` (состояния ламп) и объект `sensors` (температура/влажность).
    *   Обновляет состояние пинов и текст на экране.

---

## 4. Frontend (Web Dashboard)
**Путь:** `frontend/`
**Стек:** Next.js (React).

### Ключевые файлы:
1.  **`components/Dashboard.js`**:
    *   Главный компонент.
    *   Использует `setInterval` для опроса сервера каждые 2 секунды (`polling`).
    *   **Optimistic UI:** При нажатии кнопки интерфейс меняется мгновенно, не дожидаясь ответа сервера. Если сервер вернет ошибку, интерфейс откатится назад.
    *   **Расчет Света:** Программно вычисляет уровень освещенности на основе количества включенных ламп: `(activeLamps / 6) * 50`.
2.  **`services/api.js`**:
    *   Обертка над `fetch`. Содержит все методы API (`fetchStatus`, `toggleLamp` и т.д.).
    *   Содержит **Mock Data** (фейковые данные), которые возвращаются, если сервер недоступен (для демо-режима).
3.  **`pages/api/alice.js`**:
    *   Webhook для Яндекс.Алисы.
    *   Парсит команды ("Включи свет", "Статус") и транслирует их в запросы к Go-серверу.

---

## 5. Mobile App (Flutter)
**Путь:** `homesass/`
**Стек:** Flutter, Provider.

### Архитектура (Provider Pattern):
1.  **`lib/providers/api_provider.dart`**:
    *   Центральное хранилище данных.
    *   `fetchSmartHomeData()`: Загружает JSON с сервера.
    *   `toggleLamp()`, `toggleRelay()`: Отправляют команды.
    *   Содержит логику автоматического обновления (`Timer.periodic`).
2.  **`lib/models/smart_home_data.dart`**:
    *   Парсинг JSON данных в Dart объекты.
    *   **Важно:** Содержит геттер `lightLevel`, который дублирует логику фронтенда для расчета освещенности (исправлено, чтобы не показывать 0%).
3.  **`lib/widgets/climate_card.dart`**:
    *   Отображает данные климата. Подписан на изменения в `ApiProvider`.

### Особенности:
*   **Навигация:** Использует именованные маршруты.
*   **Темы:** Поддержка темизации через `ThemeProvider`.
*   **Надежность:** Если сохраненный URL сервера недоступен или неверен (старый IP), приложение автоматически пробует дефолтный адрес `192.168.50.9`.

---

## 6. Как это работает вместе (Workflow)

1.  **Датчики:** ESP1 считывает температуру 25°C -> Отправляет на Go Сервер.
2.  **Сервер:** Обновляет переменную `latestData`.
3.  **Web/Mobile:** При очередном опросе (через 2 сек) получают обновленный JSON и перерисовывают интерфейс.
4.  **Управление Светом:**
    *   Пользователь нажимает кнопку в приложении.
    *   Приложение шлет `POST` на Go Сервер.
    *   Сервер обновляет массив `lampCommands`.
    *   ESP2 (Lighting) при следующем опросе видит изменение и включает пин.
5.  **RFID Вход:**
    *   Пользователь подносит карту к ESP1.
    *   ESP1 шлет UID на сервер.
    *   Сервер проверяет `cards.json`. Если OK -> Включает лампу №1 (приветственный свет) и обновляет `last_access`.

---

## 7. Контакты и Поддержка
Проект разработан как демонстрация Full-Stack IoT системы.
**Автор:** Terentii Iulian
