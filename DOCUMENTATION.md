# Полная Техническая Документация Проекта "Smart Home"

## 1. Введение
Данный проект представляет собой полнофункциональную систему "Умного дома" (IoT), объединяющую аппаратную часть (микроконтроллеры), веб-интерфейс и мобильное приложение. Система позволяет пользователям мониторить климатические условия, управлять освещением и безопасностью в реальном времени.

---

## 2. Структура Проекта
Проект разделен на три независимых модуля, каждый из которых отвечает за свою часть экосистемы:

### 📂 `/esp32` и `/esp2` (Hardware / Firmware)
Прошивка для микроконтроллеров ESP32 или ESP8266.
*   `src/main.cpp`: Основной файл логики. Здесь инициализируется WiFi, поднимается HTTP-сервер, опрашиваются датчики и управляются GPIO пины.
*   `platformio.ini`: Конфигурационный файл для сборки проекта через PlatformIO (указание платформы, скорости порта, зависимостей).

### 📂 `/frontend` (Web Dashboard)
Веб-приложение для браузера, написанное на **Next.js (React)**.
*   `pages/index.js`: Точка входа.
*   `components/Dashboard.js`: Основной "мозг" фронтенда. Содержит всю логику опроса сервера и управления состоянием.
*   `services/api.js`: Слой абстракции для HTTP-запросов к микроконтроллеру.
*   `components/Icons.js`: Набор SVG иконок.

### 📂 `/homesass` (Mobile App)
Нативное мобильное приложение на **Flutter**.
*   `lib/main.dart`: Точка входа, настройка темы и провайдеров.
*   `lib/providers/`: Управление состоянием (State Management).
    *   `api_provider.dart`: Связь с ESP32.
    *   `auth_provider.dart`: Логика входа, регистрации и хранения сессии пользователя.
*   `lib/screens/`: Экраны приложения (Login, Main, Account, Splash).

---

## 3. Протокол Взаимодействия (API)
Все части системы общаются по протоколу HTTP (REST-like). Микроконтроллер выступает в роли **Сервера**, а приложения — в роли **Клиентов**.

### Базовый URL
По умолчанию: `http://192.168.50.9:8080` (настраивается в приложении).

### Эндпоинты (Endpoints)

#### 1. Получение Статуса
*   **Метод:** `GET`
*   **URL:** `/status`
*   **Ответ (JSON):**
    ```json
    {
      "temperature": 24.5,
      "humidity": 45.0,
      "soil_moisture": 60,
      "light_level": 300,
      "relay_state": false,
      "lamps": [0, 1, 0, 0, 0, 1] // Массив состояний 6 ламп (0 - выкл, 1 - вкл)
    }
    ```

#### 2. Переключение Реле (Кондиционер)
*   **Метод:** `POST` или `GET`
*   **URL:** `/relay/toggle`
*   **Описание:** Инвертирует текущее состояние главного реле.

#### 3. Управление Лампой
*   **Метод:** `POST` или `GET`
*   **URL:** `/lamp/set`
*   **Параметры:**
    *   `id`: Индекс лампы (0-5).
    *   `state`: Желаемое состояние (1 или 0).
*   **Пример:** `/lamp/set?id=2&state=1` (Включить лампу №3).

---

## 4. Детальная Логика Работы

### 🔌 Аппаратная Часть (ESP32 Logic)
1.  **Setup:** При запуске контроллер подключается к жестко заданному WiFi (SSID/Password).
2.  **Loop:**
    *   Каждые N миллисекунд считываются данные с датчиков (DHT, Soil, Photoresistor).
    *   Если поступает HTTP-запрос, выполнение основного цикла прерывается для обработки запроса.
3.  **Управление пинами:** При получении команды `/lamp/set`, контроллер меняет напряжение (HIGH/LOW) на соответствующем цифровом пине.

### 💻 Web Frontend (Next.js Logic)
1.  **Polling (Механизм опроса):**
    *   Так как ESP32 — простое устройство, оно не поддерживает сложные WebSocket соединения в данной реализации.
    *   Вместо этого браузер **каждые 2 секунды** отправляет запрос `GET /status`.
    *   Это реализовано через `setInterval` в `Dashboard.js`.
2.  **Optimistic UI (Оптимистичный интерфейс):**
    *   Когда пользователь нажимает "Включить свет", интерфейс **не ждет** ответа от сервера.
    *   Он мгновенно меняет цвет иконки на желтый.
    *   Затем отправляется запрос на сервер.
    *   Если сервер вернет ошибку, интерфейс "откатит" изменения обратно.
    *   *Зачем это нужно?* Чтобы интерфейс казался мгновенным и отзывчивым, даже если сеть медленная.
3.  **Расчет показателя "Свет":**
    *   В коде (`Dashboard.js`) есть специальная формула:
        `Итоговый Свет = Значение Датчика + (Кол-во включенных ламп / 6) * 50`.
    *   Это программная эмуляция: система "предполагает", что если вы включили лампы, в комнате стало светлее, и искусственно завышает показания датчика освещенности на дашборде.

### 📱 Mobile App (Flutter Logic)
1.  **Архитектура Provider:**
    *   Приложение использует паттерн **Provider** для управления состоянием. Это позволяет обновлять UI (например, текст температуры) в любой части приложения, как только данные изменились в `ApiProvider`.
2.  **Эмуляция Аутентификации (`AuthProvider`):**
    *   Поскольку у ESP32 нет базы данных пользователей, авторизация происходит **на стороне клиента**.
    *   `register()`: Принимает имя/email, "сохраняет" их в память и выдает фейковый токен.
    *   `login()`: Проверяет наличие данных.
    *   `logout()`: Очищает данные из памяти телефона.
    *   Данные пользователя живут пока приложение запущено (в реальном проекте они сохранялись бы в SecureStorage или на внешнем Cloud Backend).
3.  **Экраны:**
    *   **LoginScreen:** Форма с валидацией (проверка email, длина пароля).
    *   **AccountScreen:** Позволяет менять имя, email и пароль. Эти изменения мгновенно отражаются в шапке профиля через `Consumer<AuthProvider>`.

---

## 5. Инструкция по Развертыванию

### Шаг 1: Прошивка Контроллера
1.  Установите **VS Code** и плагин **PlatformIO**.
2.  Откройте папку `esp32`.
3.  В файле `src/main.cpp` найдите строки `const char* ssid` и `password`. Укажите данные вашего WiFi.
4.  Подключите ESP32 по USB.
5.  Нажмите стрелку "Upload" (в нижней панели PlatformIO).
6.  Откройте "Serial Monitor", чтобы увидеть IP-адрес устройства (например, `192.168.1.100`).

### Шаг 2: Запуск Сайта (Frontend)
1.  Убедитесь, что установлен **Node.js**.
2.  Откройте терминал в папке `frontend`.
3.  Выполните команды:
    ```bash
    npm install
    npm run dev
    ```
4.  Откройте `http://localhost:3000`.
5.  Зайдите в "Настройки" и укажите IP-адрес из Шага 1.

### Шаг 3: Запуск Приложения (Mobile)
1.  Убедитесь, что установлен **Flutter SDK**.
2.  Откройте терминал в папке `homesass`.
3.  Выполните:
    ```bash
    flutter pub get
    flutter run
    ```
4.  При первом запуске вы увидите экран входа.
5.  Нажмите "Register", введите любые данные (это эмуляция).
6.  Попав на главный экран, приложение начнет искать ESP32 по дефолтному адресу. Если не найдет — покажет ошибку, нужно будет указать верный IP в коде или настройках (если добавлены).

---

## 6. Решение проблем (Troubleshooting)

*   **Ошибка: "Connection Refused" или бесконечная загрузка.**
    *   *Причина:* Телефон/компьютер и ESP32 находятся в разных WiFi сетях, или указан неверный IP.
    *   *Решение:* Проверьте IP адрес в Serial Monitor и убедитесь, что устройства в одной сети.

*   **Данные обновляются с задержкой.**
    *   *Причина:* Интервал опроса (Polling) установлен на 2-5 секунд.
    *   *Решение:* Это нормальное поведение для HTTP-polling архитектуры. Для мгновенного обновления требуется переписывать прошивку на WebSockets.

*   **Не сохраняется пароль после перезагрузки приложения.**
    *   *Причина:* В текущей версии `AuthProvider` хранит данные в оперативной памяти (RAM) для демонстрации.
    *   *Решение:* Для продакшна нужно подключить `SharedPreferences` или `FlutterSecureStorage`.
